/** * Diese Klasse hält eine Aufzählung aller Befehlswörter, * die dem Programm bekannt sind. * * @author  Michael Kölling und David J. Barnes * @version 31.07.2011 */public class Befehlswoerter{    // Ein konstantes Array mit den gültigen Befehlswörtern    private static final String gueltigeBefehle[] = {        "neu", "suche", "liste", "hilfe", "ende",    };    /**     * Konstruktor für Befehlswoerter     */    public Befehlswoerter()    {    }    /**     * Prüfe, ob der gegebene String ein gültiges Befehlswort ist.     * @param einString der zu prüfende String.     * @return true, wenn er gültig ist, false sonst.     */    public boolean istBefehl(String einString)    {        if(einString != null){            for(int i = 0; i < gueltigeBefehle.length; i++) {                if(gueltigeBefehle[i].equals(einString))                    return true;            }        }        // Schleife komplett durchlaufen: nicht gefunden.        return false;    }    /**     * Gib alle gültigen Befehle auf System.out aus.     */    public void alleAusgeben()     {        for(String befehl : gueltigeBefehle) {            System.out.print(befehl + "  ");        }        System.out.println();    }}